[include]
	path = ~/.gitconfig.local

[core]
	editor = nvim
	precomposeunicode = true
	quotepath = false

[init]
	templatedir = ~/.git_template

[difftool]
	prompt = false

[difftool "meld"]
	cmd = meld $LOCAL $REMOTE
[difftool "p4merge"]
	cmd = /Applications/p4merge.app/Contents/Resources/launchp4merge "$LOCAL" "$REMOTE"
[mergetool "meld"]
	cmd = meld $LOCAL $BASE $REMOTE --auto-merge -o $MERGED
[mergetool "p4merge"]
	cmd = /Applications/p4merge.app/Contents/Resources/launchp4merge $BASE $LOCAL $REMOTE $MERGED
[diff]
  tool = meld
[merge]
	tool = meld

[alias]
	difft = difftool --dir-diff
	merget = mergetool --dir-diff
	br = branch
	cm = commit
	co = checkout
	cp = cherry-pick
	diffw = diff --word-diff # 行ごとの差分じゃなくて，単語レベルでの差分を色付きで表示する
	lo = log --color=always --pretty=format:'%Cgreen%h %Creset%cd %Cblue%cn %Cred%d %Creset%s'
	st = status
	stu = status -uno # Untracked filesを表示せず，not stagedと，stagedだけの状態を出力する
	graph = log --graph --date=short --decorate=short --pretty=format:'%Cgreen%h %Creset%cd %Cblue%cn %Cred%d %Creset%s'
[difftool "sourcetree"]
	cmd = /Applications/p4merge.app/Contents/MacOS/p4merge \"$LOCAL\" \"$REMOTE\"
	path = 
[mergetool "sourcetree"]
	cmd = /Applications/p4merge.app/Contents/MacOS/p4merge \"$BASE\" \"$LOCAL\" \"$REMOTE\" \"$MERGED\"
	trustExitCode = true
