eval "$(sheldon source)"

# sheldonで遅延ロードしているため不要
# source $ZDOTDIR/options.zsh
# source $ZDOTDIR/plugins.zsh
# source $ZDOTDIR/functions.zsh
# source $ZDOTDIR/alias.zsh

# # ローカルPCでだけ使いたい設定
# [ -f $ZDOTDIR/local.zsh ] && source $ZDOTDIR/local.zsh

